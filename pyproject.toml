[project]
name = "aegis-assistant-shell"
version = "0.1.1"
description = "Shell scaffold for the AEGIS AI assistant."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
aegis = "aegis.cli:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

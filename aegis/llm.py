class LlmClient:
    """Placeholder LLM client. Replace with real provider integration."""

    def generate(self, system_prompt: str, user_prompt: str) -> str:
        return (
            "AEGIS shell response (stub).\n"
            f"[system]\n{system_prompt}\n"
            f"[user]\n{user_prompt}"
        )
